/**
* @return {void} - Just a function for setting initial error on
* the argument provided for heightify.
*
*/

export default function initialError() {
  throw new Error(`Expected an object as an argument`)
}
